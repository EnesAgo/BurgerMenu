backend:
  name: github
  repo: EnesAgo/BurgerMenu      # your GitHub repo (username/repo)
  branch: main               # your main branch
  site_domain: localhost:3000  # the domain where your site is published

media_folder: "frontend/public/uploads"   # Where images go (in repo)
public_folder: "frontend/src/uploads"        # How they’re accessed from the site

collections:
  - name: "products"
    label: "Products"
    folder: "frontend/content/products"
    create: true
    slug: "{{slug}}"
    fields:
      - { name: "title", label: "Title", widget: "string" }
      - { name: "date", label: "Date", widget: "datetime" }
      - { name: "body", label: "Body", widget: "markdown" }
      - { name: "image", label: "Image", widget: "image" }
